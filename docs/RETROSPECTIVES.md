# bushel-mobile — Retrospectives

Phase-end retros, written by `/retro`. Each phase close also runs the **web↔app parity pass** (DEC-050): which admin capabilities bushel shipped this window should the app pick up. This downstream repo owns that obligation.

_(No retros yet — Phase 1 in progress.)_
