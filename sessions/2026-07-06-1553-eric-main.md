---
session: 1
dev: eric
slug: main
branch: main
started: 2026-07-06T15:53:55Z
ended: 2026-07-11T17:53:54Z
points: 2
pr_numbers: [5]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-bushel-mobile/cf17682d-d2c9-4daf-97ea-7688124ae0c3.jsonl
---

# Session 1 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 1 groundwork — CI, test/lint toolchain, QA+deploy runbook

**Completed:**
- `.github/workflows/ci.yml` — typecheck + lint + unit tests gate every PR
- jest-expo (`jest.config.js` + `src/lib/__tests__/harness.smoke.test.ts`), eslint flat config (`eslint.config.js`), `test`/`typecheck` scripts
- Committed `expo-env.d.ts` (un-ignored) so CI typechecks without booting Metro; `tsconfig` `types:["jest"]`; `engines.node >=20`
- Scoped eslint-disable in `src/hooks/use-color-scheme.web.ts` (hydration guard)
- `docs/QA_AND_DEPLOYMENT.md` — testing layers, base-URL/preview strategy, EAS→sideload, iOS/$99 truth
- Verified bushel prod serves `/api/mobile/*` (curl → 401)

**Code review:** Clean bar one real gap, addressed — `experiments.typedRoutes` defs are Metro-generated/gitignored, so CI typechecks the looser `Href` until routes land; documented in ci.yml + runbook with the fix. Minor notes deferred/anchored.
**PR:** [#5](https://github.com/mobiustripper42/bushel-mobile/pull/5)
**Points:** 2
**Branch:** task/1.0-ci-and-qa-runbook
**Opened at:** 2026-07-10T04:49:33Z

**Next Steps:**
- Issue #1 (Phase 1.1, 5 pts): email-code login + read-only active-orders screen. Branch off after PR #5 merges (or stack on it). In-app dev "Server URL" override field; default base `https://order.baybranchfarm.com`. Pure-logic unit tests (api client, token store); add `@testing-library/react-native` + react-test-renderer@19.2.3 only when a test renders a screen. Add `expo export` to CI before typecheck once route code exists.

**Context:**
- Fresh scaffold needed `npm install` before anything (node_modules absent).
- `react-test-renderer` won't co-install: latest wants `react@^19.2.7`, SDK 57 pins `react@19.2.3`. Pin it to `19.2.3` when component-render tests are actually needed; deferred for now.
- Scaffold didn't typecheck out of the box: `expo-env.d.ts` is Metro-generated + gitignored, so we committed a static one (CSS decls) and set `tsconfig types:["jest"]`. `expo lint` also needed a scoped disable in `use-color-scheme.web.ts`.
- Bushel prod (`order.baybranchfarm.com`) serves `/api/mobile/*` (401 without auth) — routes from bushel PR #282, contract in bushel `docs/API.md`. Base path is `/api/mobile/*`, not `/api/*`.
- iOS deferred hard: no simulator on Linux; $99 Apple acct is the real gate for iOS push (not just the 7-day limit). Don't buy hardware/pay yet.
