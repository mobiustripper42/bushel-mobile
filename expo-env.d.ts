/// <reference types="expo/types" />

// NOTE: Expo normally auto-generates this on `expo start` and gitignores it. We
// commit it (see .gitignore) so CI can typecheck without booting Metro. The
// expo/types reference declares *.css / *.module.css imports.
